# Copy to .env and fill in. Never commit .env.

# VITE_* variables are inlined into the production bundle at build time
# and are therefore PUBLIC. Never put a secret behind this prefix.
VITE_API_URL=http://localhost:3000/api
